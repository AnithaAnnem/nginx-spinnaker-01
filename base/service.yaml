apiVersion: v1
kind: Service
metadata:
  name: ${parameters.app}
  namespace: ${parameters.namespace}
spec:
  type: LoadBalancer
  selector:
    app: ${parameters.app}
  ports:
    - port: 80
      targetPort: 80
